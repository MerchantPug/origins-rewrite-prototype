plugins {
    id 'fabric-loom'
}

version = project.origins_version

base {
    archivesName = "origins"
}

evaluationDependsOn(":apoli")

dependencies {
    implementation(project(path: ":calio", configuration: "namedElements"))
    implementation(project(path: ":apoli", configuration: "namedElements"))
    include(project(":apoli"))
}